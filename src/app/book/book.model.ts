export class Book {
    title: string;
    author: string;
    description: string;
    genre: string;

    constructor(title: string, author: string) {
        this.title = title;
        this.author = author;
    }
}